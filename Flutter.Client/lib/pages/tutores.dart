import 'package:flutter/cupertino.dart';

class Tutores extends StatelessWidget {
  const Tutores({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Center(
        child: Text("Tutores"),
      ),
    );
  }
}
