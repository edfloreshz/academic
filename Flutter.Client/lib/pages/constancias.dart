import 'package:flutter/cupertino.dart';

class Constancias extends StatelessWidget {
  const Constancias({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Constancias"),
    );
  }
}
